action_endpoint:
    url: "http://action_server:5055/webhook"

tracker_store:
    type: SQL
    dialect: "mysql+pymysql"
    url: mysql
    db: "edu"
    username: "root"
    password: ${MYSQL_ROOT_PASSWORD}
